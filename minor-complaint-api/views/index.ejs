<!DOCTYPE html>
<html>
  <head>
    <title>Complaint Tracker</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap"
      rel="stylesheet"
    />

    <style>
      body {
        font-family: "Poppins", "Lato", sans-serif;
      }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body>
    <main class="min-h-screen bg-gray-100">
      <nav class="max-w-6xl mx-auto p-4 flex items-center justify-between">
        <div class="text-black font-poppins">
          <h1 class="text-xl font-medium">ResolveIT</h1>
        </div>
        <div class="flex items-center justify-center gap-4 text-sm">
          <a
            href="/complaints"
            class="text-black font-poppins cursor-pointer hover:text-zinc-600 transition-colors"
          >
            Complaints
          </a>
          <a
            href="/admin"
            class="px-4 py-1.5 bg-zinc-800 font-medium text-white rounded-lg hover:bg-zinc-700 transition-colors"
          >
            Admin
          </a>
        </div>
      </nav>
      <header class="text-zinc-900 max-w-6xl mx-auto p-4 mt-10">
        <div class="flex flex-col">
          <h1 class="text-5xl font-medium font-poppins">
            Complaints In. <span class="text-zinc-500">Solutions Out.</span>
          </h1>
          <p class="text-zinc-600 mt-2 text-sm font-lato">
            Report issues easily and stay updated until they’re solved.
          </p>
        </div>
      </header>
      <section class="max-w-6xl mx-auto p-4 mt-10">
        <form
          action="/api/complaints"
          method="POST"
          class="flex flex-col gap-2 max-w-2xl"
        >
          <label
            htmlFor="title"
            class="block text-sm font-medium text-zinc-700"
          >
            Title
          </label>
          <input
            type="text"
            name="title"
            placeholder="Enter complaint title..."
            required
            class="w-full p-2 border border-zinc-300 rounded-lg outline-none focus:border-zinc-500 text-zinc-900 transition-all ease-in-out duration-300"
          />

          <label
            htmlFor="description"
            class="block text-sm font-medium text-zinc-700"
          >
            Description
          </label>
          <textarea
            name="description"
            placeholder="Describe your issue clearly..."
            class="w-full h-32 p-2 border border-zinc-300 rounded-lg outline-none focus:border-zinc-500 text-zinc-900 transition-all ease-in-out duration-300"
            required
          ></textarea>

          <button
            type="submit"
            class="px-4 py-2 bg-zinc-800 text-white rounded-lg hover:bg-zinc-700 transition-colors cursor-pointer w-fit mt-4 font-poppins text-sm"
          >
            Submit Complaint
          </button>
        </form>
      </section>
    </main>
  </body>
</html>
